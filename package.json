{
  "name": "memora-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=front\" \"npm run dev --workspace=back\"",
    
    "build": "npm install && npm run build --workspace=front",
    
    "start": "npm start --workspace=back"
  },
  "workspaces": [ "front", "back" ],
  "dependencies": { "path": "^0.12.7" },
  "devDependencies": { "concurrently": "^8.2.2" }
}